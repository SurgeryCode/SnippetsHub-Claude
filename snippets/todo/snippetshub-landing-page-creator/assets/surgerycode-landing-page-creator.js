(()=>{
  function init(){
    // placeholder for future interactions (accordion enhancements, smooth scroll, etc.)
  }
  if(document.readyState==='loading') document.addEventListener('DOMContentLoaded',init); else init();
})();
